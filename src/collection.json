{
  "schematics": {
    "core": {
      "aliases": [ "cor" ],
      "description": "Generates schematic Core Store module for ngrx",
      "factory": "./core",
      "schema": "./core/schema.json"
    },
    "store": {
      "aliases": [ "sto" ],
      "description": "Generates schematic Core Store module with ngrx",
      "factory": "./store",
      "schema": "./store/schema.json"
    },
    "features": {
      "aliases": [ "fea" ],
      "description": "Generates schematic Feature Store module with ngrx",
      "factory": "./features",
      "schema": "./features/schema.json"
    }
  }
}
